export type Reply = {
  id: string;
  questionId: string;
  answerId: string;
  content: string;
  authorId: string;
  authorName: string;
  createdAt: string; // ISO
  updatedAt: string; // ISO
};










